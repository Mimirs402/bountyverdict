# Copy these names to ~/.config/bountyverdict/github-nomination.env.
# Keep the real file outside Git and set its mode to 0600.
SMTP_USERNAME=admin@mimirslab.com
SMTP_TOKEN=replace-with-a-dedicated-proton-smtp-token
SEND_GITHUB_MCP_NOMINATION=YES
