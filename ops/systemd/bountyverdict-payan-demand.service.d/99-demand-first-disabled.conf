[Unit]
Description=BountyVerdict Payan mutation lane disabled by demand-first policy

[Service]
# Fail closed even if another unit or an operator starts the service directly.
ExecCondition=/usr/bin/false
